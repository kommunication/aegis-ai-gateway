# 01 — curl Basics

Step-by-step curl walkthrough of every gateway endpoint.

## What it shows

- Health check, model listing, chat completions
- Authentication with API keys
- Request/response format (OpenAI-compatible)
- Error handling (invalid model, missing auth, bad input)

## Status

Planned — run `demos/00-quickstart` for now and follow the curl examples in the root README.
