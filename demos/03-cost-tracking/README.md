# 03 — Cost Tracking

Shows per-request cost calculation and aggregated reporting.

## What it shows

- `estimated_cost_usd` in every response
- Per-model cost breakdown in PostgreSQL (`usage_records` table)
- Prometheus cost metrics (`aegis_cost_usd_total`)
- Cost comparison across providers for the same prompt

## Status

Planned.
